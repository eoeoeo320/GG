# exam-memory 프로젝트 규칙

## 빈칸/키워드 출력 규칙

1. 출력은 번호(①~⑮) 구조를 그대로 유지한다.
2. 빈칸은 반드시 해당 위치에 정확히 1개 개념만 채운다.
3. 중복 개념을 다른 번호에 반복해서 넣지 않는다.
4. 각 이론은 고유 키워드만 사용한다.
5. 구조를 바꾸거나 설명을 추가하지 않는다.
6. 추측하지 말고 교재 내용에서만 선택한다.
7. 문장은 제공된 문장을 그대로 사용한다.

## 빈칸 생성 방식

### 작동 방식
1. 문장을 분석하여 핵심 키워드를 1~2개 선택한다.
2. 선택 기준:
   - 개념/이론명
   - 핵심 결과/결론
3. 문장 구조는 절대 변경하지 않는다.
4. 선택된 키워드만 [빈칸]으로 치환한다.
5. 나머지는 그대로 유지한다.

### 출력 형식
- 한 줄로 출력한다. 줄바꿈 없음.
- `개념(이론)명: 원문(일부 핵심어만 [빈칸] 처리)` 형태로 출력한다.
- 마침표, 설명 추가 금지.
- 적합한 핵심어가 없으면 원문을 그대로 출력한다.

### 예시
입력:
```
절대우위론: 각 국가의 절대우위 산업에 특화하여 교역하면 양국의 총 생산량이 증가한다.
```
출력:
```
절대우위론: 각 국가의 [빈칸] 산업에 특화하여 교역하면 양국의 총 생산량이 증가한다.
```

## 사용자 지정 키워드 빈칸 처리

### 입력 형식
- 개념 설명 문장
- 빈칸 처리할 키워드 리스트 (사용자가 지정)

### 규칙
1. 문장은 절대 변경하지 않는다.
2. 지정된 키워드만 정확히 [빈칸] 처리한다.
3. 다른 단어는 절대 빈칸으로 만들지 않는다.
4. 추가 설명 금지.
5. 출력은 문제만.
6. 문장의 구조와 어순은 반드시 유지한다.
7. 조사(의, 가, 을/를, 은/는)와 연결 표현(으로, 을/를 통해, 함으로써)은 빈칸 처리하지 않는다.
8. 문장 전체를 빈칸으로 만들 수 없다.

### 출력 형식
```
개념명:
원문 문장 (지정 키워드만 [빈칸])
```
